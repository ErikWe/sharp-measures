# Utility: true

# Requires: UnitUtility

# Offset
<summary>The bias, or offset, of the #ReferenceToUnit/#.</summary>
/#

# Constructor
<summary>Constructs a new #ReferenceToUnit/#, where a unit step represents <paramref name="#ParameterNameOfQuantity/#"/>, and with an
<paramref name="offset"/>.</summary>
<param name="#ParameterNameOfQuantity/#">The <see cref="#ReferenceToQuantity/#"/> that a unit step of #ReferenceToUnit/# represents.</param>
<param name="offset">The bias, or offset, of the #ReferenceToUnit/#.</param>
/#

# OffsetBy_Scalar
<summary>Derives a new #ReferenceToUnit/# from this instance, with an offset of <paramref name="offset"/> - where the offset represents the
value of the new #ReferenceToUnit/# when the original #ReferenceToUnit/# is zero.</summary>
<param name="offset">The value of the new #ReferenceToUnit/# when the original #ReferenceToUnit/# is zero.</param>
<remarks>When deriving a #ReferenceToUnit/#, the order of <see cref="OffsetBy(Scalar)"/> and <see cref="ScaledBy(Scalar)"/> matters.</remarks>
/#

# OffsetBy_Double
#OffsetBy_Scalar/#
/#